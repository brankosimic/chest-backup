{
  "name": "@chest-backup/app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc",
    "start": "docker compose up",
    "start:now": "docker compose build && docker compose run --rm chest-backup -- --run-now",
    "start:dry": "docker compose run --rm chest-backup -- --dry-run",
    "test": "bun test --bail --timeout 120000 --path-ignore-patterns 'dist/**'",
    "test:unit": "bun test --bail --path-ignore-patterns 'dist/**' test/unit/",
    "test:e2e": "docker compose -f docker-compose.test.yaml down --remove-orphans && docker compose -f docker-compose.test.yaml build test-runner && docker compose -f docker-compose.test.yaml run --rm test-runner",
    "lint": "eslint src/",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "cron-parser": "^5.0.1",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0",
    "ssh2-sftp-client": "^12.1.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/bun": "latest",
    "@types/ssh2-sftp-client": "^9.0.6",
    "eslint": "^9.17.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0"
  }
}
