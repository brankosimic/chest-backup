const row = "group flex cursor-pointer items-center gap-2 px-4 py-2 hover:bg-muted/50"
const iconSlot = "flex w-4 shrink-0 items-center justify-center"
const spacer = "flex h-4 w-4 shrink-0 items-center justify-center text-muted-foreground"
const info = "min-w-0 flex-1 space-y-0.5"
const title = "break-words font-medium"
const chevron = "h-4 w-4 text-muted-foreground transition-transform"
const label = "min-w-0 flex-1 break-words font-medium"
const count = "text-xs text-muted-foreground"
const meta = "flex shrink-0 items-center gap-2"
const deleteButton =
  "rounded p-1 text-muted-foreground opacity-0 transition-all hover:text-destructive group-hover:opacity-100"
const detailText = "text-xs text-muted-foreground"
const trashIcon = "h-3.5 w-3.5"

export { row, iconSlot, spacer, chevron, label, info, title, count, meta, deleteButton, detailText, trashIcon }
