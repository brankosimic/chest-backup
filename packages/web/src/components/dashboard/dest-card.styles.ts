const header = "flex flex-row items-center justify-between space-y-0 pb-2"
const title = "text-sm font-medium text-muted-foreground truncate"
const badge = "shrink-0"
const content = "space-y-2"
const statRow = "flex items-baseline gap-4"
const statGroup = "space-y-1"
const statValue = "text-lg font-bold"
const statLabel = "text-xs text-muted-foreground"
const footer = "flex items-center gap-1.5 border-t pt-2 text-xs text-muted-foreground"
const footerValue = "font-medium text-foreground"
const cardShell = "relative transition-colors hover:border-primary"
const unavailableIcon = "absolute right-2 top-2 h-4 w-4 text-destructive"
const skeletonWide = "h-7 w-16 animate-pulse rounded bg-muted"
const skeletonNarrow = "h-7 w-10 animate-pulse rounded bg-muted"
const skeletonTextWide = "h-3 w-16 animate-pulse rounded bg-muted"
const skeletonTextNarrow = "h-3 w-10 animate-pulse rounded bg-muted"
const clockIcon = "h-3 w-3"
const linkShell = "block"

export {
  header,
  title,
  badge,
  content,
  statRow,
  statGroup,
  statValue,
  statLabel,
  footer,
  footerValue,
  cardShell,
  unavailableIcon,
  skeletonWide,
  skeletonNarrow,
  skeletonTextWide,
  skeletonTextNarrow,
  clockIcon,
  linkShell,
}
