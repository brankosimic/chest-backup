const page = "mx-auto max-w-5xl"
const errorPage = "mx-auto max-w-7xl"
const sectionHeader = "mb-3 flex items-center gap-2 border-b pb-2"
const sectionTitle = "text-lg font-semibold"
const sectionCount = "text-sm text-muted-foreground"
const treePanel = "rounded-lg border"
const emptyCard = "pt-6"
const errorCard = "flex flex-col items-center gap-4 pt-6"
const emptyText = "text-center text-muted-foreground"
const emptyDesc = "mt-2 text-center text-sm"

const sections = "space-y-8"
const plusIcon = "mr-2 h-4 w-4"

export { page, errorPage, sectionHeader, sectionTitle, sectionCount, treePanel, emptyCard, errorCard, emptyText, emptyDesc, sections, plusIcon }
