# Log level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# Data directory for app state (backup history, etc.)
# Defaults to ~/.local/share/chest-backup if unset
# CHEST_DATA_DIR=/var/lib/chest-backup
